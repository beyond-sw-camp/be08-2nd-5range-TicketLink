<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beyond.ticketLink.autono.persistence.mapper.AutoNoMapper">

    <select id="getData" resultType="AutoNo">
        select GET_NO(#{tableName}) as autono FROM DUAL
    </select>
</mapper>